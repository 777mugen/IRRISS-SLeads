# 架构原则 (Architecture Principles)

**最后更新**: 2026-03-14
**决策人**: 董胜豪

---

## 核心原则

### 1. 原始数据优先 (Raw Data First)

**原则**: 
- ✅ **保留所有原始数据**（图片、链接、完整HTML结构）
- ✅ **提取和处理逻辑放在后续环节**（智谱批处理、解析器）
- ✅ **实现最大的灵活性**

**原因**:
- 原始数据一旦丢失，无法恢复
- 不同的处理需求可能需要不同的数据
- 后续处理可以灵活调整，但原始数据必须完整

**应用场景**:
- **Jina Reader API**: 保留所有图片、链接、完整Markdown结构
- **数据库存储**: raw_markdown 表存储完整的原始内容
- **智谱批处理**: 从原始数据中提取结构化信息

---

## 数据管道架构

```
┌─────────────────┐
│  Jina Reader    │ → 保留所有原始数据（图片、链接、完整HTML结构）
│  (Raw Data)     │
└─────────────────┘
        ↓
┌─────────────────┐
│  raw_markdown   │ → 存储原始 Markdown（包含所有信息）
│  (Storage)      │
└─────────────────┘
        ↓
┌─────────────────┐
│  智谱批处理      │ → 提取结构化信息（标题、作者、地址等）
│  (Extraction)   │
└─────────────────┘
        ↓
┌─────────────────┐
│  paper_leads    │ → 存储结构化数据（提取后的信息）
│  (Storage)      │
└─────────────────┘
        ↓
┌─────────────────┐
│  评分/导出/应用  │ → 业务逻辑（评分、导出CSV、Feishu通知）
│  (Business)     │
└─────────────────┘
```

---

## 具体实现

### Jina Reader API 参数配置

**原则**: 保留所有原始数据

```python
headers = {
    'X-Retain-Links': 'all',    # ✅ 保留所有链接（图片链接、邮箱链接等）
    'X-Retain-Images': 'all',   # ✅ 保留所有图片
    'X-Remove-Selector': (
        'nav, aside, footer, .sidebar, '
        '.advertisement, .comments, '
        '.related-articles, .social-share'
    ),  # ✅ 只删除导航栏、广告等无关元素
}
```

**注意**:
- ❌ **不要**在 Jina Reader 环节删除任何数据
- ✅ **可以**删除导航栏、广告等无关元素（这些不影响数据提取）

---

### 智谱批处理 Prompt

**原则**: 从原始数据中提取结构化信息

**Prompt 设计要点**:
- 从 Markdown 文本中提取作者姓名（忽略图片链接）
- 从 Markdown 文本中提取邮箱（忽略 `mailto:` 链接）
- 从 Markdown 文本中提取地址（忽略图片）
- 识别通讯作者标注（*、#、†等符号）
- 翻译机构地址为中文

---

### 数据清洗逻辑

**原则**: 在后续环节处理

**位置**: `src/processors/batch_result_parser.py`

**处理逻辑**:
1. 解析智谱批处理结果（JSON）
2. 验证字段完整性
3. 清洗数据（去除多余空格、格式化等）
4. 转换数据类型（日期、数组等）

---

## 决策记录

| 日期 | 决策 | 原因 | 决策人 |
|------|------|------|--------|
| 2026-03-14 | 保留所有原始数据（图片、链接） | 实现最大的灵活性 | 董胜豪 |
| 2026-03-14 | 提取和处理逻辑放在后续环节 | 数据清洗在应用层更灵活 | 董胜豪 |

---

## 相关文档

- `docs/jina_api_parameters.md` - Jina API 参数配置说明
- `docs/Batch Prompt v2.md` - 智谱批处理 Prompt 设计
- `src/crawlers/jina_client.py` - Jina Reader API 实现
- `src/processors/batch_result_parser.py` - 数据清洗逻辑
